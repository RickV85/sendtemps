import React from 'react';
import '../../home.css';

function PullToRefreshContent(): JSX.Element {
  return (
    <div className="pull-to-refresh-container">
      <p>Pull down to refresh forecast</p>
    </div>
  );
}

export default PullToRefreshContent;
